---
---

<div id="container" class="flex min-h-screen items-center justify-center px-4 py-12 font-sans text-purple-950 sm:px-6">
  <main class="w-full max-w-7xl flex justify-center">
    <section id="login" class="w-full max-w-md rounded-3xl border border-purple-100/60 bg-purple-50/85 p-6 shadow-2xl shadow-purple-950/15 backdrop-blur-sm sm:p-10">
      <h1 class="mb-8 text-center text-3xl font-bold tracking-tight text-purple-950">
        Login
      </h1>
      <form action="http://localhost:3000/login" method="POST" class="space-y-6">
        <div class="space-y-2">
          <label for="email" class="block text-sm font-semibold text-purple-900">
            Email:
          </label>
          <input
            type="email"
            id="email"
            name="email"
            autocomplete="email"
            required
            class="block h-12 w-full rounded-xl border border-purple-200 bg-purple-50 px-4 text-base text-purple-950 outline-none transition placeholder:text-purple-600 hover:border-purple-400 focus:border-indigo-600 focus:ring-4 focus:ring-indigo-200"
          />
        </div>

        <div class="space-y-2">
          <label for="password" class="block text-sm font-semibold text-purple-900">
            Password:
          </label>

          <input
            type="password"
            id="password"
            name="password"
            autocomplete="current-password"
            required
            class="block h-12 w-full rounded-xl border border-purple-200 bg-purple-50 px-4 text-base text-purple-950 outline-none transition placeholder:text-purple-600 hover:border-purple-400 focus:border-indigo-600 focus:ring-4 focus:ring-indigo-200"
          />
        </div>

        <button
          type="submit"
          class="flex h-12 w-full items-center justify-center rounded-xl bg-indigo-700 px-5 text-sm font-semibold text-purple-50 transition hover:bg-indigo-800 focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-indigo-200 active:bg-indigo-900"
        >
          Login
        </button>
    </form>
  <div class="mt-6 flex flex-col items-center gap-1 text-center text-sm text-purple-800">
		<span>New here?</span>
    <a href="/register" class="font-semibold text-indigo-700 underline hover:text-indigo-900">
			Create an account
		</a>
	</div>
     
    </section>
  </main>
</div>
