---
import Button from './Button.astro';
---

<div id="container" class="flex min-h-screen items-center justify-center bg-blue-50 px-4 py-12 text-blue-950 sm:px-6">
  <main class="flex w-full max-w-7xl justify-center">
    <section class="w-full max-w-md rounded-3xl border border-purple-100/60 bg-purple-50/85 p-6 shadow-2xl shadow-purple-950/15 backdrop-blur-sm sm:p-10">
      <h1 class="font-h1---desk mb-4 text-center text-purple-800">
        Create account
      </h1>
      <p class="font-p---lg mb-8 text-blue-950">
        Save your dream travel destinations in one place
      </p>
      <form action="http://localhost:3000/register" method="POST" class="space-y-6">
        <div class="space-y-2">
          <label for="email" class="font-h6---desk block text-blue-950">
            Email:
          </label>
          <input
            type="email"
            id="email"
            name="email"
            autocomplete="email"
            required
            class="block h-12 w-full rounded-xl border border-blue-200 bg-blue-50 px-4 text-base text-blue-950 outline-none transition placeholder:text-blue-500 hover:border-indigo-400 focus:border-indigo-600 focus:ring-4 focus:ring-indigo-200"
          />
        </div>

        <div class="space-y-2">
          <label for="password" class="font-h6---desk block text-blue-950">
            Password:
          </label>
          <input
            type="password"
            id="password"
            name="password"
            minlength="12"
            autocomplete="new-password"
            required
            class="block h-12 w-full rounded-xl border border-blue-200 bg-blue-50 px-4 text-base text-blue-950 outline-none transition placeholder:text-blue-500 hover:border-indigo-400 focus:border-indigo-600 focus:ring-4 focus:ring-indigo-200"
          />
          <p class="font-p---sm text-blue-500">Use at least 12 characters.</p>
        </div>

        <div class="space-y-2">
          <label for="confirm-password" class="font-h6---desk block text-blue-950">
            Verify password:
          </label>
          <input
            type="password"
            id="confirm-password"
            name="confirmPassword"
            minlength="12"
            autocomplete="new-password"
            required
            class="block h-12 w-full rounded-xl border border-blue-200 bg-blue-50 px-4 text-base text-blue-950 outline-none transition placeholder:text-blue-500 hover:border-indigo-400 focus:border-indigo-600 focus:ring-4 focus:ring-indigo-200"
          />
        </div>

        <Button type="submit">
          Create account
        </Button>
      </form>

      <p class="mt-6 flex flex-col items-center gap-1 text-center text-purple-800">
        <span class="font-p---sm">Already have an account?</span>
        <a href="/login" class="font-p---sm font-semibold text-indigo-700 underline hover:text-indigo-900">Sign in</a>
      </p>
    </section>
  </main>
</div>
